# weblens
WebLens is a grounded RAG-based web research assistant designed to make AI answers verifiable. The system decomposes complex queries, performs parallel web retrieval, processes and chunks live webpages, and combines sparse retrieval, dense embeddings, and reranking pipelines to surface the most relevant evidence before generating responses.
