# ⚠️ DISABLED: Use Railway's native GitHub integration instead
# GitHub Actions deployment kept failing with token issues
# Railway's built-in integration is more reliable
#
# Setup: Railway Dashboard → Project Settings → GitHub Integration → Connect Repository
#
# name: Deploy to Railway
#
# on:
#   push:
#     branches: [ main ]
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#
#     steps:
#       - uses: actions/checkout@v3
#
#       - name: Deploy to Railway
#         run: |
#           npm install -g @railway/cli
#           railway up
#         env:
#           RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
